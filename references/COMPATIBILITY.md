# 兼容性与重新验证

## 已完成真实验证

- Windows 11 x64；
- 企业微信 5.0.11.6018；
- `WXWork.exe` SHA-256：`ca7280999c0ae011bf2fac296c24f796595f3558df784edc50dd3f9a6edac5d9`；
- 核心库：`message.db`、`session.db`、`user.db`；
- 页面格式：4096 字节 wxSQLite3 AES-128-CBC；
- 在线内存捕获候选必须通过目标数据库第一页验证；
- 真实端到端测试覆盖精确群名、48 小时时间窗、141 条本地消息、三个数据库完整性、批次复核和离线 HTML/PNG。

这些信息是兼容性证据，不是密钥，也不保证相同版本号的所有分发构建完全一致。

## 新构建验证

客户端可执行文件摘要与上述记录不同时，不要直接宣称兼容。完成以下检查后才能报告成功：

1. `doctor` 记录新的客户端路径和 SHA-256；
2. 候选密钥通过目标第一页验证；
3. 所有核心库格式、WAL 与完整性检查通过；
4. 实际表结构满足显式适配器；
5. 精确群名、时间边界、消息计数和去重不变量通过；
6. 完整批次总结、HTML、PNG、移动/桌面布局通过；
7. 确认没有明文数据库或密钥文件。

如果表结构变化，应新增明确适配和虚构回归测试；不要猜测字段。
